class Card {
    Id: number;
    Name: string;
    Desc: string;

    constructor(Id: number, Name: string, Desc: string) {
        this.Id = Id;
        this.Name = Name;
        this.Desc = Name;
    }
}

export default Card;